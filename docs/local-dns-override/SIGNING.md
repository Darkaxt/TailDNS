# Fork identity and validation signing

App: **TailDNS**, package `io.github.darkaxt.taildns`; navigation scheme `taildns://navigate`. Exported VPN broadcasts use the fork package prefix. The official app is not replaced and its credentials are not migrated. Both apps can be installed, but Android permits only one active VPN per user; switching VPNs is a deliberate live-test action.

Public signing certificate SHA-256:

```text
dcd0ede91eb7e0ed88a72b5289f84f8d4a61ccc22707f36378e15540b99d32e1
```

The persistent independent RSA-4096 identity was provisioned with `scripts/provision-fork-signing.ps1`. Local custody is `%LOCALAPPDATA%\TailDNS\signing`, restricted to the provisioning Windows user. `taildns.jks` is the primary key; `password.dpapi.xml` is protected by that Windows account's DPAPI. `signer.der` is public. Do not delete this directory during build cleanup or rotate it during updates.

Recovery on this Windows account: rerun the provisioning script to validate the existing key and re-upload the encrypted GitHub secrets. The script rejects a missing key/password pair rather than generating a replacement. DPAPI is account/machine-bound: migration to another machine needs a deliberate secure password/key export before losing the old account. GitHub secrets cannot be downloaded as a backup. No plaintext password copy is created.

GitHub secrets: `TAILDNS_KEYSTORE_BASE64`, `TAILDNS_KEYSTORE_PASSWORD`. Public variable: `TAILDNS_SIGNER_SHA256`. Signing jobs must compare the APK certificate to this pin. Fork pull requests cannot access these secrets.

The manual `TailDNS validation build` workflow only runs on this fork's `main`. Build/test and signing run on separate fresh runners; no repository code or build plugins run with secrets. It uploads a signed validation artifact, not a release. Candidate version codes are the workflow run number times ten. The final release pipeline must preserve this identity and allocate monotonically increasing version codes across both validation and release builds.

Native-reliability candidate [35366855618](https://github.com/Darkaxt/tailscale-android/actions/runs/35366855618) passed build/test/lint and isolated signing. Independent Windows SDK verification matched the certificate pin, package `io.github.darkaxt.taildns`, code 80, version `1.103.260-t423bcd55b-gec2ba1d53`, target SDK 36 and four ABIs. SHA-256 `eb24bf61930e4ed740dc403f37a41d91bf7d3c42379c1302177499474005f517`; Android source `ec2ba1d53938f63754e87e1a53f0d943b01a0e6a`, core `423bcd55b5cce99e7a3bb4ed78b276e3f8ad77e0`. The in-place Thor update and available Guard-disabled matrix are recorded in [ANDROID-VALIDATION.md](ANDROID-VALIDATION.md).

Always-on foreground candidate [35370729766](https://github.com/Darkaxt/tailscale-android/actions/runs/35370729766) passed the same clean build/test/lint and isolated-signing pipeline. Independent Windows SDK verification matched the certificate pin, package `io.github.darkaxt.taildns`, code 90, version `1.103.260-t423bcd55b-gd9ba4b231`, target SDK 36 and all four ABIs. SHA-256 `58d6cc72cc7d8abaa89c7380620f69dc4306f6b76ea8b1c7b770c53ad11a82e2`; Android source `d9ba4b23152f7421ddad8e0d9a899b3c6b4e5b75`, core `423bcd55b5cce99e7a3bb4ed78b276e3f8ad77e0`. Its in-place Thor system-start, foreground-process, resolver, TUN-replacement and Wi-Fi evidence is recorded in [ANDROID-VALIDATION.md](ANDROID-VALIDATION.md). It remains a validation artifact, not the public feature release.

Autosave candidate [35361257936](https://github.com/Darkaxt/tailscale-android/actions/runs/35361257936) passed the same CI/signing and independent download checks. Package `io.github.darkaxt.taildns`, code 60, version `1.103.260-t423bcd55b-g7d71a9b78`, target SDK 36; signer matches the pin above. SHA-256 `d7d39450c84dfffd4e4ff688b3d84828e10b0482404d68ca3389f9518bd33256`. Android source `7d71a9b78b738d41632925a72fec5804469279da`, core `423bcd55b5cce99e7a3bb4ed78b276e3f8ad77e0`. In-place Thor installation preserved login; verified behavior and remaining gates are in [ANDROID-VALIDATION.md](ANDROID-VALIDATION.md). No public feature release was created.

Automatic-follow candidate [35357288592](https://github.com/Darkaxt/tailscale-android/actions/runs/35357288592) passed CI and isolated signing. Independent `apksigner` verification matched the certificate above; `aapt` reported the fork package, version code 50, `1.103.257-t304c9f715-g4463d8d8b`, target SDK 36. Downloaded SHA-256 `0ac13c96769ab229354f0064dc0949412d568f971b86c85fe5dedbfeeda34e7e` matched its manifest. Android source `4463d8d8b9efb6712b96353af64cef52bfbf9329`, core `304c9f7157b74405a5037748a617aaaa5f75fe53`. In-place Thor installation preserved login and enabled the checks in [ANDROID-VALIDATION.md](ANDROID-VALIDATION.md). It is not a feature release.

Core provenance: `require tailscale.com` and its fork replacement must pin the same version. `scripts/fork-mkversion.sh` supplies the fork's Git history to upstream version generation, which otherwise ignores replacements. The embedded core hash must match the resolved module. For local builds, `TS_MKVERSION_OSS_GIT_CACHE` may point at the existing `Darkaxt/tailscale` checkout; otherwise an isolated `taildns-core` version cache is created. A different-origin cache is rejected, not repurposed.

Status: validation run [35350671598](https://github.com/Darkaxt/tailscale-android/actions/runs/35350671598) passed build/test and isolated signing. Independent Windows SDK 36 `apksigner verify` and `aapt dump badging` verified the downloaded APK against the fingerprint above, package `io.github.darkaxt.taildns`, version code `30`, version `1.103.255-t811a1030f-gfffdf00c3`, target SDK 36 and four ABIs. SHA-256: `d7f6c9731d55554a686a2a6f06d220083660622bd8652b5bd6407256a00d7f0e`, matching the workflow checksum. Android source: `fffdf00c3f9e6c52117a9a4eca16749c20ed1534`; core: `811a1030f5e4a5e007b9354572e463fe9dcef037`. This is build/signature evidence, not installed-device evidence. The production tag/release workflow and post-validation update automation belong to later stages and are not claimed complete here.
